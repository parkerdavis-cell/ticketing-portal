# Server Configuration
PORT=3000
NODE_ENV=production

# Future: Database connection (when upgrading from JSON file storage)
# DATABASE_URL=postgresql://user:password@localhost:5432/ticketing

# Future: Email service configuration
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASS=your-sendgrid-api-key
# EMAIL_FROM=support@scottshipsolutions.com
